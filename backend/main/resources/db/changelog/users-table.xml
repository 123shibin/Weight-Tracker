<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="
    http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- ORIGINAL TABLE CREATION (DO NOT EDIT) -->
    <changeSet id="0001" author="shibin">
        <createTable tableName="users">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true"/>
            </column>
            <column name="email" type="VARCHAR(255)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="password" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

    <!-- NEW CHANGESET TO ADD FULLNAME -->
    <changeSet id="0002" author="shibin">
        <addColumn tableName="users">
            <column name="fullname" type="VARCHAR(255)">
                <constraints nullable="true"/>
            </column>
        </addColumn>
    </changeSet>
    
    <changeSet id="0003" author="shibin">

    <createTable tableName="weights">

        <column name="id" type="BIGINT" autoIncrement="true">
            <constraints primaryKey="true"/>
        </column>

        <column name="weight_value" type="DOUBLE">
            <constraints nullable="false"/>
        </column>

        <column name="date" type="DATE">
            <constraints nullable="false"/>
        </column>

        <!-- FK user_id -->
        <column name="user_id" type="BIGINT">
            <constraints nullable="false"/>
        </column>

    </createTable>

    <!-- Foreign Key Constraint -->
    <addForeignKeyConstraint
            baseTableName="weights"
            baseColumnNames="user_id"
            referencedTableName="users"
            referencedColumnNames="id"
            constraintName="fk_weight_user"/>

    <!-- Unique Constraint (1 weight per day per user) -->
    <addUniqueConstraint
            tableName="weights"
            columnNames="user_id,date"
            constraintName="uk_user_date"/>

</changeSet>

</databaseChangeLog>
